# SafeWatch Backend — Python dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database (async)
sqlalchemy==2.0.36
aiosqlite==0.20.0          # SQLite driver (dev)
asyncpg==0.30.0            # PostgreSQL driver (prod)
alembic==1.14.0            # Migrations

# Auth / crypto
bcrypt==4.2.0
python-jose[cryptography]==3.3.0

# Validation
pydantic[email]==2.10.2
python-multipart==0.0.12   # form data

# HTTP client (used by edge script, handy for tests too)
requests==2.32.3
httpx==0.28.0              # async test client
