# SafeWatch

Fall and bed-exit detection system with:

- FastAPI backend in `backend`
- Alembic migrations for Supabase/Postgres
- React/Vite dashboard in `safewatch-ui`
- Jetson Nano edge detector in `edge`

Deployment instructions are in [DEPLOYMENT.md](DEPLOYMENT.md).
